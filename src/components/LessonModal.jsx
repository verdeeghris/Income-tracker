import { useEffect, useState } from 'react'
import { Check, Copy, X } from 'lucide-react'
import { BTN_GHOST_CLASS, BTN_PRIMARY_CLASS, INPUT_CLASS, MAX_COPIES, PALETTE, paletteOf } from '../constants'
import { CopyPicker, presetMonthEnd, presetNextWeeks } from './ProjectModal'
import { formatLongDate } from '../dateUtils'

export default function LessonModal({ dateKey, instance, templates = [], isDark, onClose, onSave, onDelete }) {
  const [title, setTitle] = useState(instance?.title || ''); const [amount, setAmount] = useState(instance?.amount ? String(instance.amount) : '')
  const [color, setColor] = useState(instance?.color || PALETTE[0].id); const [isPaid, setIsPaid] = useState(Boolean(instance?.isPaid)); const [copyDates, setCopyDates] = useState(new Set()); const [picker, setPicker] = useState(false)
  useEffect(() => { const previous = document.body.style.overflow; document.body.style.overflow = 'hidden'; const handler = (event) => event.key === 'Escape' && onClose(); window.addEventListener('keydown', handler); return () => { document.body.style.overflow = previous; window.removeEventListener('keydown', handler) } }, [onClose])
  const toggle = (key) => setCopyDates((previous) => { const next = new Set(previous); if (next.has(key)) next.delete(key); else if (next.size < MAX_COPIES) next.add(key); return next })
  const apply = (keys) => setCopyDates((previous) => { const next = [...new Set(keys)].filter((key) => key !== dateKey); return previous.size === next.length && next.every((key) => previous.has(key)) ? new Set() : new Set(next) })
  const submit = (event) => { event.preventDefault(); const parsed = Number(amount); if (!title.trim() || !Number.isFinite(parsed) || parsed <= 0) return; onSave({ title: title.trim(), amount: parsed, color, isPaid, copyDates: [...copyDates].sort() }) }
  const templatesForLessons = templates.filter((item) => item.type !== 'project')
  return <div className="modal-overlay" onClick={onClose}><div className="modal-sheet" onClick={(event) => event.stopPropagation()}><div className="mb-4 flex items-start justify-between gap-3"><div><h2 className="text-base font-extrabold">{instance ? 'Редактировать урок' : 'Новый урок'}</h2><p className="mt-0.5 text-xs font-medium text-stone-500 dark:text-zinc-400">{formatLongDate(dateKey)} · без налога</p></div><button type="button" onClick={onClose} aria-label="Закрыть" className="rounded-lg p-2 text-stone-400 hover:bg-stone-100 dark:hover:bg-white/5"><X /></button></div><form onSubmit={submit} className="flex flex-col gap-4">{templatesForLessons.length > 0 && <div><label className="mb-1.5 block text-[11px] font-bold uppercase tracking-wider text-stone-400 dark:text-zinc-500">Использовать прошлый урок</label><div className="flex gap-1.5 overflow-x-auto p-1">{templatesForLessons.map((item) => { const c = paletteOf(item.color); return <button type="button" key={item.title} onClick={() => { setTitle(item.title); setAmount(String(item.amount)); setColor(item.color) }} className="shrink-0 rounded-full border px-3 py-1.5 text-xs font-bold" style={{ backgroundColor: isDark ? c.darkBg : c.bg, borderColor: isDark ? c.darkBorder : c.border, color: isDark ? c.darkText : c.text }}>{item.title}<Copy className="ml-1 inline h-3 w-3" /></button> })}</div></div>}<label className="flex flex-col gap-1.5 text-[11px] font-bold uppercase tracking-wider text-stone-400 dark:text-zinc-500">Название урока<input required maxLength={80} value={title} onChange={(event) => setTitle(event.target.value)} placeholder="Например, консультация" className={INPUT_CLASS} /></label><label className="flex flex-col gap-1.5 text-[11px] font-bold uppercase tracking-wider text-stone-400 dark:text-zinc-500">Оплата<input required inputMode="numeric" value={amount} onChange={(event) => setAmount(event.target.value.replace(/\D/g, '').slice(0, 9))} placeholder="3 500" className={INPUT_CLASS} /><span className="normal-case tracking-normal text-stone-500 dark:text-zinc-400">Уроки не облагаются налогом: сумма учитывается полностью.</span></label><div><label className="mb-1.5 block text-[11px] font-bold uppercase tracking-wider text-stone-400 dark:text-zinc-500">Скопировать на другие дни</label><div className="flex flex-wrap gap-1.5"><button type="button" onClick={() => apply(presetNextWeeks(dateKey))} className={BTN_GHOST_CLASS}>+4 недели</button><button type="button" onClick={() => apply(presetMonthEnd(dateKey))} className={BTN_GHOST_CLASS}>До конца месяца</button><button type="button" onClick={() => setPicker(!picker)} className={BTN_GHOST_CLASS}>Вручную</button>{copyDates.size > 0 && <span className="rounded-full bg-emerald-100 px-2.5 py-1 text-[11px] font-bold text-emerald-700 dark:bg-emerald-400/10 dark:text-emerald-300">{copyDates.size} дн.</span>}</div>{picker && <CopyPicker anchorKey={dateKey} selectedSet={copyDates} onToggle={toggle} />}</div><div><label className="mb-1.5 block text-[11px] font-bold uppercase tracking-wider text-stone-400 dark:text-zinc-500">Цвет карточки</label><div className="flex flex-wrap gap-2">{PALETTE.map((item) => <button type="button" key={item.id} aria-label={`Цвет: ${item.label}`} onClick={() => setColor(item.id)} className={`size-8 rounded-lg border ${color === item.id ? 'ring-2 ring-stone-800 ring-offset-2 dark:ring-white' : ''}`} style={{ backgroundColor: isDark ? item.darkBg : item.bg, borderColor: isDark ? item.darkBorder : item.border }} />)}</div></div><label className="flex cursor-pointer select-none items-center gap-2.5 py-1"><input type="checkbox" checked={isPaid} onChange={(event) => setIsPaid(event.target.checked)} className="peer sr-only" /><span className="flex size-5 items-center justify-center rounded-md border border-stone-300 bg-stone-50 peer-checked:border-stone-900 peer-checked:bg-stone-900 dark:border-white/20 dark:bg-white/5 dark:peer-checked:border-white dark:peer-checked:bg-white [&>svg]:opacity-0 peer-checked:[&>svg]:opacity-100"><Check className="size-3.5 stroke-[3] text-white dark:text-zinc-900" /></span><span className="text-sm font-bold">Выплачено</span></label><div className="flex justify-end gap-2 border-t border-stone-100 pt-4 dark:border-white/5">{instance && <button type="button" onClick={onDelete} className="mr-auto rounded-xl px-3 py-2 text-xs font-bold text-rose-500">Удалить</button>}<button type="button" onClick={onClose} className={BTN_GHOST_CLASS}>Отмена</button><button type="submit" className={BTN_PRIMARY_CLASS}>Сохранить</button></div></form></div></div>
}
